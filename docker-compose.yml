services:
  mongodb:
    image: mongo:7.0
    container_name: todo_mongodb
    ports:
      - "27017:27017"
    command: ["--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data:
